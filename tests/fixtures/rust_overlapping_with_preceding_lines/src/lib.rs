pub fn has_overlapping() {
    let something_before = whoo()
        .ok()
        .this_isnt_real_code()
        .i_promise()
        .but_it_has_to_be_longer();
    let f = || {
        || {
            println!("whee");
        }
    };
}
